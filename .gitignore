# Rust build artifacts
/target

# Copied binary (single-file deployment convenience)
/spinyarn

# Mappings are not committed; SpinYarn auto-downloads the full 43+39 set on
# startup (see maven.auto_download). Download manually via scripts/.
/mappings/

# ZeroTermux Editor GUI scaffold (not part of the project)
/build.sh

# Logs & temp
*.log
*.log.txt
*.tmp

# Keep real-log test samples
!tests/fixtures/*.log
!tests/fixtures/*.log.txt

# Editor / OS
.idea/
.vscode/
.DS_Store
