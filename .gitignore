# Rust build artifacts
/target

# Copied binary (single-file deployment convenience)
/spinyarn

# Vanilla (Mojang official) mappings are large (~300MB); download via
# scripts/download_vanilla_mappings.py instead of committing them.
/mappings/vanilla/

# ZeroTermux Editor GUI scaffold (not part of the project)
/build.sh

# Logs & temp
*.log
*.log.txt
*.tmp

# Keep real-log test samples
!tests/fixtures/*.log
!tests/fixtures/*.log.txt

# Editor / OS
.idea/
.vscode/
.DS_Store
